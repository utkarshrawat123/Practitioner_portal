node_modules/
.next/
data/
.env*.local
.env
tsconfig.tsbuildinfo
.vercel
.env*.local
.superpowers/
.open-next/
.wrangler/

# Cloudflare local dev secrets (wrangler reads this for `wrangler dev`).
# NEVER commit: it holds the same values as production secrets.
.dev.vars
.dev.vars.*
